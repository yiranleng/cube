# cube_dbt package - cube_dbt 包参考

## 文章重点总结

- **cube_dbt** 将 **dbt** 模型与 Cube 数据模型桥接：从 dbt manifest、catalog 等生成或补全 cubes（sql、列、测试等）。在 requirements.txt 添加 `cube_dbt`，在 YAML 或 Python 中调用其工具函数，可减少手写、保持与 dbt 一致。详见 Data modeling / Recipes / Using Cube with dbt。
