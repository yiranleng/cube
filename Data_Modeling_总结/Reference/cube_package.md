# cube package - cube 包参考

## 文章重点总结

- **cube** 为内置包，提供 `cube()`、`view()`、`context()` 等 API 以及 **TemplateContext**（Python/Jinja）、**SafeString** 等；在 model、cube.py、globals 中可直接使用，无需在 requirements.txt 声明。参考文档列出可用符号与用法。
