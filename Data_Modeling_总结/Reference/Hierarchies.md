# Hierarchies - 层级参考

## 文章重点总结

- **hierarchies** 将多个 **dimensions** 组织成层级（如 country → region → city），便于报表中逐级下钻。在 cube 的 **hierarchies** 中列出层级名与 **levels**（维度名有序数组）。View 中可通过 **includes** 包含整个 hierarchy 或其中某一 level。
