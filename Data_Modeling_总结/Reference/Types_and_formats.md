# Types and formats - 类型与格式参考

## 文章重点总结

- **Dimension types**：time、string、number、boolean 等；与 SQL 类型有常见对应。**Measure types**：count、sum、avg、count_distinct、min、max、number（含百分位）等。**format**：在 measure/dimension 上控制展示，如 `currency`、`percent`、`id`、自定义。时间格式、数字精度等可在 format 或类型参数中配置。
